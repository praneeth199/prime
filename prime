#include<stdio.h>
int isprime(int q)
{
	for(int i=2;i<q;i++)
	{
		if(q%i==0)
		
			return 0;
		}
		 
		}

int main()
{
int a,result;
printf("enter the value");
scanf("%d",&a);
result=isprime(a);

if(result==0)


	printf("composite");
else 
printf("prime");
return 0;
}
